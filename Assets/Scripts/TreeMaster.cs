using System.Collections;
using System.Collections.Generic;
using UnityEngine;

#region Enums
    enum TreeShape{Straight, Tilt, Hook};
    enum LeafShape{Stringy, Round, Drapes};
#endregion

public class TreeMaster : MonoBehaviour
{
    // Publics

    // Privates
    private List<Color> lstColors;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
